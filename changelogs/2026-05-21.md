# Changelog — 2026-05-21

## ✅ Teléfono movido a Step1Account (Opcional)

- **Step1Account.tsx**: Agregado campo de teléfono opcional en el formulario de registro inicial
  - Campo `phone` agregado al estado inicial
  - PhoneInput con `helpText="Opcional"` y sin `required`
  - Teléfono capturado antes de crear la cuenta en Memberstack

- **Step4CompleteProfile.tsx**: Eliminado campo de teléfono completamente
  - Campo `phone` removido del estado inicial y de la carga de datos existentes
  - Validación de teléfono removida de `validateForm`
  - Componente `<PhoneInput />` eliminado del JSX
  - Import de PhoneInput eliminado del archivo

## QA & Build
- `npm run type-check` ✅
- `npm run build` ✅

## ✅ Teléfono guardado en Supabase confirmado

- **registerUserInSupabase** (user.actions.ts): ya tenía `phone: userData.phone` en el objeto dataToSave
  - El teléfono viaja: Step1Account → handleStep1Complete → account.phone → registerUserInSupabase → dataToSave.phone → INSERT/UPDATE en tabla `users`
  - Filtro cleanedData respeta phone: se envía a Supabase solo si `phone !== ''`

- **handleStep4Complete** (NewRegistrationFlow.tsx): actualiza `phone` en Memberstack
  - Si `profileData.phone` existe, se incluye en customFields: `msUpdateFields.phone = profileData.phone`

- **Step4CompleteProfile.tsx**: phone ahora inicializado desde `data.account.phone` y reenviado en dataToSubmit

## QA & Build
- `npm run type-check` ✅
- `npm run build` ✅


## ✅ Visualización de Información del Centro de Bienestar

- **public/widgets/wellness-center-widget.js**:
  - Agregadas clases de estilo CSS premium a `STYLES`.
  - Agregadas constantes con iconos SVG nativos para Instagram, Facebook, TikTok y Website.
  - Actualizada la función `renderDashboard` para renderizar el logo (o inicial en su defecto), nombre comercial, razón social, datos de contacto (teléfono, email, dirección física con enlace a Google Maps por coordenadas) y beneficio para miembros.
  - Agregados los botones de redes sociales activas con efectos hover interactivos.

## QA & Build
- `npm run type-check` ✅
- `npm run lint` ✅
- `npm run build` ✅


## ✅ Consistencia de Diseño en Fondo de Registro de Centros de Bienestar

- **src/app/bienestar/registro/page.module.css**:
  - Actualizado el color de fondo general de `.pageBackground` para usar el mismo gradiente suave del flujo de miembros (`linear-gradient(180deg, #F7FAFC 0%, #EDF2F7 100%)`).
  - Agregado el pseudo-elemento `::before` con la ilustración de la mascota en marca de agua (`LetraApalo_3_bjsdfo.png`) con `opacity: 0.2` y sin interrupción de interacción.
  - Asignado `z-index: 1` a `BrandLogo` y `.container` para que permanezcan legibles y clickeables por encima de la decoración.

## QA & Build
- `npm run type-check` ✅
- `npm run lint` ✅
- `npm run build` ✅


