# Changelog - 2026-05-13

## Ambassador Dashboard Metrics Optimization

### Added
- Real-time referral counts in `GET /api/ambassadors/by-memberstack`.
- Status-specific counts: `approved_referrals` (approved + paid), `review_referrals` (pending), and `rejected_referrals` (cancelled).
- Empty state UI in the ambassador widget for when no referrals exist.

### Changed
- **Ambassador Widget**: Removed all hardcoded mock data (earnings, statistics, and referrals list).
- **API**: Enhanced the ambassador status endpoint to include detailed statistics and robust referral counting.
- **Logic**: Updated the widget to use `total_referrals` and `total_earnings` directly from the database response.

### Fixed
- Mock data appearing even for new ambassadors with zero activity.
- Fallback values for card information and referral codes.

---
*Last updated by AI Agent*
