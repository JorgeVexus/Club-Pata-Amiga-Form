# Changelog - 2026-05-08

## [Solidarity Fund Dashboard] Final Stabilization & Consistently Enforced Logic

### Fixed
- **ERR_CONNECTION_CLOSED**: Replaced all failing `via.placeholder.com` references with reliable internal assets (`animated-dog-image-0929.png`) across the entire widget.
- **Strict Eligibility**: Updated `calculateCarencia` and the backend `/api/solidarity/stats` to be stricter—if a pet's creation date or waiting period end date is missing, it is automatically treated as "Pending" instead of "Active".
- **Search & Filter Visibility**: Forced `color: black !important` and added hover transitions to history search and filters to ensure readability in all environments.
- **Data Integrity**: Added debug logs for tracking pet counts to resolve the "phantom add pet button" issue.

### Added
- `plan-fondo-solidario.md`: A permanent record of the implementation strategy and eligibility rules for future maintenance.

### Technical Details
- Modified `public/widgets/solidarity-dashboard.js`.
- Modified `src/app/api/solidarity/stats/route.ts`.
- Created `plan-fondo-solidario.md`.
- Verified production build and types with `npm run build` and `npm run type-check`.

---

## [Solidarity Fund Request] Mobile Optimization & Data Persistence

### Added
- **Mobile Solidarity Widget**: Fully responsive, multi-step support request flow with scroll-lock, anti-overflow measures, and app-like transitions.
- **Data Persistence Architecture**: Created `allied_centers` table and extended `solidarity_requests` to support 100% of the form fields (including Medical Emergencies).
- **Allied Centers API**: New endpoint `/api/solidarity/allied-centers` to dynamically populate the provider list.
- **Multi-Document Support**: Implemented structured metadata storage for evidence, prescriptions, and receipts in Supabase.

### Fixed
- **Horizontal Overflow**: Resolved layout shifts caused by decorative icons in narrow viewports.
- **Scroll Hijacking**: Implemented automated body scroll-lock during success modal active state.
- **User Mapping**: Fixed resolution of Memberstack IDs to Supabase UUIDs in backend solidarity requests.

### Technical Details
- Modified `public/widgets/solidarity-request-form.js`.
- Modified `src/app/api/solidarity/request/route.ts`.
- Created `src/app/api/solidarity/allied-centers/route.ts`.
- Created migration `supabase/migrations/20260509_extend_solidarity_fields.sql`.
- Modified `src/lib/supabase.ts`.
