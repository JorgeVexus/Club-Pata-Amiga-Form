# Changelog — 2026-07-17

## Consistencia visual de vistas internas del Admin V2

- Se publico el commit `0a02906` (`style: align admin internal views with v2`) en `origin/main`.
- Se retiraron los bordes negros gruesos y las sombras desplazadas del estilo brutalista en los modulos internos.
- El sidebar ahora fuerza la tipografia `Outfit`, con mayor contraste y legibilidad frente a los estilos globales de produccion.
- Se conservaron los colores semanticos, handlers, permisos, tablas y acciones administrativas existentes.
- Verificacion: 13 pruebas aprobadas, type-check, lint sin errores y build de 150 paginas exitoso.

## Dashboard administrativo V2

- Se publico el commit `1d2d01a` (`feat: redesign admin dashboard shell`) en `origin/main`.
- Se adapto el shell del dashboard administrativo al nuevo estandar visual sin reemplazar el orquestador, permisos, rutas, modales ni acciones existentes.
- Se agrego un resumen operativo conectado a las metricas y colas actuales, junto con un estado de carga consistente y una capa visual encapsulada para las vistas internas.
- La metrica de reintegros ahora suma montos aprobados reales en lugar de utilizar una simulacion derivada de miembros.
- Se conservo la navegacion profunda y las restricciones por rol administrativo.
- Se agrego un preview local, documentacion y 11 pruebas de regresion del Admin V2.
- Se excluyo del lint el repositorio visual de referencia `temp-pata-amiga`, que no forma parte de la aplicacion desplegada.

### Verificacion

- 11 de 11 pruebas dirigidas aprobadas.
- `npm run type-check`: exitoso.
- `npm run lint`: exitoso, sin errores; conserva advertencias historicas.
- `npm run build`: exitoso, 150 paginas generadas.

## `1b74540` — `fix: persist member welcome once per account`

- Se agrego la migracion idempotente `users.welcome_shown` para persistir la bienvenida por cuenta.
- El endpoint de bienvenida ahora confirma la fila actualizada y devuelve error si la cuenta no existe.
- El widget solo cierra permanentemente el modal despues de una escritura exitosa y permite reintentar si falla.
- El modal adopta el estilo de `WelcomeOnce` del repositorio visual nuevo.
- El icono usa SVG y los textos visibles entidades HTML para evitar caracteres corruptos en produccion.
- Se agregaron pruebas de regresion para migracion, persistencia, estilo y mojibake.

### Impacto funcional

La bienvenida deja de repetirse despues de marcarse como vista y mantiene el estado entre navegadores y dispositivos una vez aplicada la migracion en Supabase.

### Verificacion

- Push confirmado a `origin/main` en `1b74540`.
- 24 pruebas dirigidas aprobadas.
- `npm run type-check`: exitoso.
- `npm run build`: exitoso, 149 rutas generadas.
- ESLint dirigido: sin errores.
- El lint global conserva dos errores preexistentes dentro de `temp-pata-amiga`.

### Operacion pendiente

- Aplicar `supabase/migrations/20260717_add_welcome_shown_users.sql` en el proyecto remoto de Supabase.

## Correccion de carga de Reintegros

- Se corrigio el contexto del `fetch` nativo en el cliente integrado y el cliente separado de Reintegros.
- Se agrego una prueba que reproduce el error de navegador `Illegal invocation` y confirma que `fetch` conserva `globalThis` como receptor.
- Push confirmado a `origin/main` en el commit `019c29a` (`fix: bind reimbursements fetch context`).

## Elegibilidad de mascotas en Reintegros

- Se publico el commit `6bbe871` (`fix: respect pet waiting period in reimbursements`) en `origin/main`.
- El formulario ahora solo habilita mascotas activas, aprobadas y con el tiempo de espera completado.
- Las mascotas en carencia permanecen visibles, deshabilitadas y muestran los dias restantes.
- El envio queda deshabilitado cuando la cuenta no tiene ninguna mascota elegible.
- Se agregaron pruebas de regresion para la carencia y el bloqueo del formulario.
- Se corrigio el texto de dias restantes para usar UTF-8 real y evitar que `d&iacute;as` aparezca literalmente despues del escapado HTML.
- Push confirmado a `origin/main` en el commit `f1dd944` (`fix: render waiting period text correctly`).

## Centros aliados en Dashboard V2

- Se integro el directorio de Centros del Bienestar como vista interna del widget unificado.
- La vista consume centros aprobados mediante `/api/wellness/locations` y conserva la arquitectura API-first para Webflow.
- Se agregaron busqueda, filtros por servicio, tarjetas responsive, estados de carga/error/vacio y reintento.
- El CTA `Quiero ser centro aliado` dirige al formulario de Home V2.
- Al vaciar la busqueda se restauran todos los resultados y `Borrar filtros` reinicia busqueda y categoria.
- Se agregaron pruebas de regresion para navegacion, carga, filtros y limpieza de estado.
- Push confirmado a `origin/main` en el commit `7a33119` (`feat: integrate allied centers directory`).

## Navegacion movil del Dashboard V2

- Se agrego una barra horizontal fija al fondo en pantallas moviles, siguiendo el patron del repositorio nuevo.
- Incluye accesos a Inicio, Peludos, Reintegros, Vet 24/7 y Centros; Embajador se muestra de forma condicional.
- La vista activa queda resaltada y el contenido reserva espacio para evitar superposiciones.
- Se agrego soporte para el area segura inferior de dispositivos moviles y una prueba de regresion.
- Push confirmado a `origin/main` en el commit `e4854a3` (`feat: add fixed mobile dashboard navigation`).

## Orientacion veterinaria y notificaciones Dashboard V2

- Se integro Orientacion veterinaria 24/7 como vista interna accesible desde la barra lateral, el menu movil y el boton superior.
- El Vet Bot conserva Memberstack, la validacion de plan activo, el token de sesion, el saludo automatico y el historial de Chatbot Builder.
- Se elimino el autoarranque como burbuja flotante y se habilito el montaje explicito dentro del contenedor del Dashboard V2.
- La campana superior consume las API Routes existentes, muestra el contador no leido y permite leer una notificacion o marcar todas como leidas.
- El dropdown, sus estados de carga/error/vacio y la vista del chat adoptan el nuevo estandar visual responsive.
- Se agregaron pruebas de regresion para navegacion interna, seguridad API-first, proteccion por plan y montaje del chat.
- Push confirmado a `origin/main` en el commit `6d46103` (`feat: embed vet guidance and notifications`).

## Dashboard de embajadores V2 y seguridad

- Se publico el commit `08d36c3` (`feat: secure ambassador dashboard v2`) en `origin/main`.
- Se adapto el widget independiente de embajadores al nuevo estandar visual, conservando sus conexiones con referidos, comisiones, pagos, materiales, perfil y administracion.
- Se agrego un endpoint consolidado y autenticado para el dashboard, con validacion JWT de Memberstack y comprobacion de propiedad en las operaciones sensibles.
- Los nombres de referidos ahora se entregan enmascarados desde el servidor para proteger la privacidad de los miembros.
- La solicitud de cambio de codigo mantiene el flujo conectado al backend y al panel administrativo, con estados y mensajes funcionales.
- Se reforzaron las rutas de mensajes, pagos, cancelacion, cambio de codigo, reenvio, perfil y subida de fotografia para impedir acceso cruzado entre embajadores.
- Se agregaron 14 pruebas de regresion para el nuevo dashboard, privacidad y seguridad de autorizacion.

## Ajuste del menú móvil de embajadores

- Se publico el commit `cbd8e18` (`fix: prevent ambassador mobile menu overflow`) en `origin/main`.
- La navegación del dashboard de embajadores ahora usa una cuadrícula de dos columnas en móvil.
- Los cuatro accesos permanecen completamente visibles sin scroll horizontal ni botones cortados.
- El comportamiento horizontal de escritorio permanece sin cambios.
- Se agrego una prueba de regresión responsive y el conjunto relacionado suma 15 pruebas aprobadas.
