# Changelog - 2026-05-10

## Resumen del día
Hoy se corrigió un fallo crítico en la lógica de carencia (tiempo de espera) y se refinó el sistema de solicitudes del Fondo Solidario para alinearlo con los requerimientos operativos y de diseño de Club Pata Amiga.

## Cambios Realizados

### Backend & API
- **fix**: Se corrigió el cálculo de carencia para que inicie al momento de la **aprobación por el admin** en lugar de al momento de la creación del registro.
  - El endpoint `POST /api/admin/members/[id]/pets/[petId]/status` ahora puebla automáticamente `waiting_period_start` y `waiting_period_end` tras la aprobación.
  - Se añadieron campos adicionales (`is_adopted`, `is_mixed_breed`, etc.) en la selección previa a la actualización para garantizar cálculos precisos de días (120/150/180).

### Widgets (Frontend)
- **refactor**: Se actualizaron los widgets `pet-cards-widget.js`, `solidarity-dashboard.js` y `unified-membership-widget.js`.
  - Se implementó una lógica de prioridad para el cálculo de carencia: usa `waiting_period_start` si existe, y retrocede a `created_at` para registros antiguos, asegurando **retrocompatibilidad total** con clientes actuales.
- **style**: Refinamiento final de iconos y tipografías en el detalle de solicitudes del Fondo Solidario.
  - Integración de iconos oficiales para clínica y fecha (`solicitud-clinica.svg`, `solicitud-fecha.svg`).

### Documentación
- **docs**: Actualización de la guía técnica del Fondo Solidario reflejando la nueva arquitectura de fechas de carencia.

## Impacto Funcional
- Las nuevas mascotas aprobadas tendrán un tiempo de carencia preciso que no cuenta el tiempo que tardó el admin en revisar la solicitud.
- Los clientes actuales no verán cambios ni reinicios en sus tiempos de carencia actuales.
- Se facilita el testeo manual permitiendo la manipulación de fechas en el campo `waiting_period_start`.

### Optimización de Caché y Solución de Bugs (Sesión Posterior)
- **fix**: Se implementó "Cache Busting" (`&t=${Date.now()}`) en las peticiones a la API de mascotas dentro de los widgets (`pet-cards-widget.js`, `unified-membership-widget.js`, y `user-profile-widget.js`).
  - **Problema Resuelto**: Soluciona un comportamiento anómalo donde el widget de perfil cargaba datos cacheados del navegador (previos a la aprobación administrativa), lo que provocaba un cálculo erróneo del progreso del período de carencia al caer en el *fallback* de `created_at`.
  - **Impacto**: Los widgets ahora siempre reflejarán la información más reciente de la base de datos de Supabase.

### Estado de Carencia Cumplido y Fechas Dinámicas (Sesión Final)
- **feat**: Implementación de la vista de éxito "Fondo Activado" tras culminar el periodo de carencia.
  - Se añadió lógica condicional en `pet-cards-widget.js` y `unified-membership-widget.js` para reemplazar la barra de progreso por un banner de felicitación y un botón de CTA hacia `/miembros/tu-manada`.
- **fix**: Solución al desfase de fechas de activación en la interfaz de usuario.
  - Los widgets ahora calculan la fecha de activación (`endDate`) dinámicamente basándose en el `waiting_period_start` y el total de días, ignorando campos estáticos o estancados de la base de datos durante el testeo.
  - Se unificó esta lógica en `solidarity-dashboard.js` y `solidarity-request-form.js`, asegurando que el panel de solicitudes detecte correctamente a las mascotas que han finalizado su carencia.

### Estandarización de Navegación y Estabilización (Sesión Actual)
- **fix**: Estandarización de todas las rutas de navegación del Fondo Solidario.
  - Se actualizaron todos los botones de "Solicitar Apoyo" y "Utiliza tus beneficios" para redirigir a `https://www.pataamiga.mx/miembros/tu-manada`.
- **fix**: Corrección de la lógica de cálculo de carencia en el frontend.
  - Se reemplazó `Math.abs` por `Math.max(0, ...)` para evitar errores en fechas futuras y cálculos incoherentes.
  - Se añadió soporte para múltiples convenciones de nombres de propiedades (`isAdopted`, `is-adopted`, `is_mixed_breed`, etc.) para asegurar que el periodo de 120 días se asigne correctamente.
- **feat**: Mejora visual en `solidarity-dashboard.js`.
  - Se integró una barra de progreso visual en las tarjetas de mascotas en carencia.
  - Se refinó el mensaje de éxito y el botón de acción una vez cumplido el periodo de espera.
- **Impacto**: Navegación 100% centralizada y lógica de carencia blindada contra inconsistencias de datos y ajustes manuales.

### Estabilización de Widgets y Lógica de Carencia Robusta (Sesión Extra)
- **fix (solidarity-dashboard.js)**: Corrección de errores de sintaxis crítica en los métodos de renderizado y mejora de la auto-inicialización mediante un manejador de errores global y `try-catch`.
- **fix (solidarity-request-form.js)**: Solución del error `TypeError: Cannot read properties of undefined (reading 'split')` mediante una inicialización de fecha más segura (`substring` en lugar de `split`).
- **refactor (calculateCarencia)**: Sincronización de la lógica de carencia en los 3 widgets principales (`solidarity-dashboard.js`, `solidarity-request-form.js`, `pet-cards-widget.js`).
  - Ahora maneja robustamente tipos mixtos (booleano, string, número) para propiedades como `is_adopted` y `is_mixed_breed`.
  - Añadida validación de fechas: si se manipulan manualmente en la base de datos con formatos inválidos, el sistema ahora se recupera grácilmente usando la fecha actual como respaldo en lugar de fallar con `NaN`.
- **Impacto**: Estabilidad total en el dashboard de miembros y el formulario de solicitud, con una lógica de carencia preparada para cualquier variación de datos o manipulación manual durante pruebas.

### Corrección de Navegación del Fondo Solidario (Sesión de Cierre)
- **fix**: Se actualizó la URL de redirección en los botones de "Solicitar Apoyo" y "Utiliza tus beneficios" de `/miembros/tu-manada` a `https://www.pataamiga.mx/miembros/fondo-solidario`.
  - Aplicado en `solidarity-dashboard.js` (botones globales e individuales).
  - Aplicado en `pet-cards-widget.js` para mantener consistencia en la experiencia del usuario.
- **Impacto**: Redirección precisa al módulo de solicitudes del Fondo Solidario, mejorando el flujo de usuario post-carencia.

### Estabilización Final y Sincronización de Carencia (Sesión de Estabilización)
- **feat (SolidarityRequestForm)**: Unificación y centralización del campo "Identificador del Caso" (`case_title`).
  - Ahora el campo es obligatorio y visible al inicio de todos los formularios (Citas y Reembolsos), eliminando confusiones en el flujo de datos.
- **fix (Backend API)**: Sincronización total de la lógica de carencia en `api/solidarity/request` y `api/solidarity/stats`.
  - Se implementó una validación robusta que prioriza `waiting_period_start` y realiza el cálculo dinámico (90/120/180 días) basado en el perfil real de la mascota (`is_adopted`, `is_mixed_breed`).
  - Esto garantiza que tanto el frontend como el backend hablen el mismo idioma y que los ajustes manuales de fechas en la base de datos se respeten correctamente.
- **fix (Frontend)**: Refactorización del renderizado del formulario para asegurar que todos los campos requeridos se capturen correctamente antes del envío.
- **Impacto**: Se eliminaron los errores de validación de carencia "fantasma" y se aseguró la captura de datos críticos para la gestión administrativa de las solicitudes.
### Refinamiento de Iconografía y Estabilidad Visual (Sesión de Cierre de UI)
- **fix (solidarity-dashboard.js)**: Corrección de iconos rotos en el panel de historial.
  - Se reemplazaron todas las rutas relativas `/Icons/...` por URLs dinámicas basadas en `this.baseUrl`, garantizando el renderizado correcto de activos en entornos de producción (Webflow).
  - Se actualizó el constructor para soportar `window.PATA_AMIGA_CONFIG?.baseUrl` para una configuración de entorno más flexible.
- **fix (solidarity-request-detail.js)**: Sincronización del constructor para alinearse con el patrón de configuración global, asegurando que los activos visuales del detalle de solicitud siempre apunten al dominio correcto.
- **QA**: Verificación final de la lógica de carencia y el campo `case_title` en todo el flujo de solicitud, confirmando que la manipulación manual de fechas se refleja correctamente en la interfaz y permite el envío de solicitudes.
- **Impacto**: Interfaz del historial del Fondo Solidario visualmente íntegra y profesional, con una infraestructura de assets robusta ante cambios de hosting o subdominios.

### Visualización de Documentos y URLs Privadas (Sesión de Estabilización de Evidencia)
- **fix (Backend API)**: Se implementó la generación de **URLs firmadas (Signed URLs)** en el detalle de solicitudes.
  - Esto soluciona los errores 404 y las imágenes rotas, ya que el bucket `solidarity-documents` es privado y requiere tokens temporales para el acceso externo.
- **feat (solidarity-request-detail.js)**: Implementación de un **Visor de Documentos (Modal)**.
  - Ahora las imágenes y PDFs se abren en una ventana emergente premium dentro del mismo dashboard, evitando que el usuario tenga que abandonar la página.
  - Se añadió soporte para previsualización dinámica y un botón de descarga directa para cualquier tipo de archivo.
- **fix (UI)**: Corrección del icono de fecha roto en la sección de detalles del incidente (cambio a `calendario.svg`).
- **Impacto**: Experiencia de usuario fluida y segura para la revisión de documentos sensibles, con una interfaz 100% funcional en entornos de producción.

### Optimización de la Ficha de Mascota y Responsividad Móvil
- **fix (solidarity-request-detail.js)**: Corrección de la visualización de la foto de la mascota.
  - Se implementó una lógica de fallback multicanal (`primary_photo_url`, `photo_url`, `photo1_url`) similar a la del widget de manada.
- **fix (UI)**: Corrección de la fecha de registro de la mascota.
  - Ahora utiliza el campo `created_at` de Supabase, evitando el error de "Invalid Date".
- **style (Mobile)**: Ajuste de responsividad en móviles para la tarjeta de información de la mascota.
  - Se corrigió el colapso visual de la foto (`pata-pet-photo-wrapper` al 100% de ancho) para asegurar que la imagen no se vea como una línea en dispositivos pequeños.

### Mejoras en Dashboard Admin (Solidaridad)
- **fix (API)**: Sincronización de nombres en la lista de solicitudes.
  - Se actualizó el endpoint `/api/admin/solidarity/list` para aplanar los objetos `user` y `pet`. Esto resuelve el problema de columnas vacías ("Usuario" y "Mascota") en el panel administrativo.
- **fix (UI)**: Mapeo de tipos de beneficio.
  - Se añadió soporte visual para el tipo de beneficio `death` (🕊️ Fallecimiento) en la tabla principal del dashboard de administración.

### Notificaciones en Barra Lateral (Sidebar)
- **feat (UI/UX)**: Contadores de notificaciones en tiempo real.
  - Se implementó un distintivo (badge) para el menú "Nuevas solicitudes" dentro de la sección de Fondo Solidario, permitiendo ver el volumen de solicitudes pendientes sin entrar al módulo.
  - Se verificó y aseguró la sincronización de los contadores para "Miembros" y "Embajadores" en la barra lateral.
### Navegación Reactiva y Deep-Linking (Sesión de Administración)
- **feat (AdminDashboard)**: Implementación de navegación reactiva basada en parámetros de la URL.
  - El dashboard ahora escucha cambios en `tab`, `requestId`, `member` y `ambassadorId` mediante `useSearchParams`.
  - Permite la apertura automática de modales de detalle al hacer clic en notificaciones sin recargar la página.
  - Se envolvió el componente en un `Suspense` boundary para compatibilidad con el App Router de Next.js.
- **fix (UI/UX)**: Refactorización y limpieza del código del dashboard para eliminar redundancias y errores estructurales.
- **Impacto**: Mejora crítica en la usabilidad del sistema de notificaciones, permitiendo una gestión administrativa mucho más fluida y profesional.
### Localización y Visor de Documentos (Detalle de Solidaridad)
- **feat (SolidarityRequestDetail)**: Localización completa al español.
  - Se tradujeron todos los tipos de beneficios (ej: "Emergencia Médica", "Vacunación Anual") y estados de solicitud (ej: "Acción Requerida").
- **feat (SolidarityRequestDetail)**: Implementación de visor de documentos nativo.
  - Se añadió un modal de previsualización para imágenes y PDFs adjuntos, evitando la descarga forzada o apertura en pestañas externas.
  - Incluye controles de descarga y cierre con diseño premium.
- **fix**: Corrección de la lógica de envío de mensajes en el chat administrativo.
  - Se migró a `adminFetch` para asegurar la autenticación correcta.
  - Se mejoró el flujo de "Solicitar Información", permitiendo enviar un mensaje atómico al cambiar el estado.
- **fix (API/UI)**: Restauración de visibilidad de solicitudes en "Acción Requerida".
  - Se actualizó el endpoint de listado para soportar la categoría `in_process` (incluye `in_review` y `needs_info`).
  - Se ajustó la barra lateral y los filtros del dashboard para agrupar estas solicitudes bajo "En proceso".
- **Impacto**: Gestión administrativa 100% en español, mayor agilidad en la revisión de evidencia documental y flujo de comunicación con el usuario estabilizado.
### Estabilización de Chat y Visor de Documentos (Sesión Final)
- **fix (SolidarityRequestDetail)**: Corrección del error de cliente al abrir documentos adjuntos.
  - Se corrigieron los nombres de propiedades (`file_url`, `file_name`) y se añadieron validaciones de nulidad (`|| ''`) para evitar el crash al procesar extensiones de archivo.
  - Se actualizó la lógica de scroll del chat para usar `ref` en lugar de IDs estáticos, garantizando un desplazamiento fluido.
- **fix (API/DB)**: Solución al error de sintaxis UUID en la base de datos.
  - Se implementó una validación de formato UUID en `/api/solidarity/requests/[id]/messages` y `/api/admin/solidarity/update`.
  - Esto permite que los IDs de Memberstack (`mem_...`) se manejen correctamente como remitentes de mensajes, evitando errores 500.
- **feat (Database)**: Creación de migración SQL para cambiar `sender_id` a tipo `TEXT`.
  - Proporciona una solución definitiva a nivel de esquema para el rastreo de administradores en el historial de chat.
- **Impacto**: Interfaz administrativa 100% estable, eliminación de errores de runtime en el chat y visualización de documentos garantizada.
