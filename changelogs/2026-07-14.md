# Changelog — 2026-07-14

## 🚀 feat: Cambio de Plan Autónomo en User Profile y Notificaciones al Admin

### 1. Cambio de Plan en User Profile Widget
- **`public/widgets/user-profile-widget.js`**
  - Se inyectaron estilos CSS premium para la selección de planes (`.pata-plan-option-card`, etc.) dentro de la etiqueta `<style>` global del widget.
  - Se agregó el enlace/botón de **"Cambiar Plan"** en la sección de información de membresía del widget (`renderSection2()`), visible únicamente cuando el miembro tiene una suscripción activa (no cancelada).
  - Se implementó el método interactivo `openChangePlanModal()` que renderiza el modal de selección de planes (Mensual vs Anual), maneja la lógica de selección visual, solicita confirmaciones amigables según la dirección del cambio (upgrade inmediato con prorrateo o downgrade diferido al final del ciclo) y procesa la petición de cambio de plan llamando al endpoint `/api/user/change-plan`.
  - Se vinculó el evento click al disparador en `bindEvents()`.

### 2. Notificación en Tiempo Real al Admin
- **`src/app/api/user/change-plan/route.ts`**
  - Se integró la función `createServerNotification` de `@/app/actions/notification.actions`.
  - Al procesarse exitosamente un cambio de plan en el servidor, se dispara una notificación persistente al administrador (`userId: 'admin'`).
  - La notificación incluye el nombre completo del miembro que realizó el cambio, el tipo de plan destino (Mensual o Anual), y vincula directamente al perfil detallado del miembro mediante el enlace `/admin/dashboard?tab=member&member=${memberstackId}` y metadatos `{ userId: memberstackId }`.

---

## 🚀 feat: Integración Lynsales (LeadConnector) y Datos de Pago/Cupones en Reportes

### 1. Extracción de Cupones y Fechas de Pago de Stripe
- **`src/lib/stripe-membership.ts`**
  - Se expandió `discount.promotion_code` en las llamadas a Stripe para recuperar de forma confiable el código de promoción o cupón (`couponCode`) aplicado a la suscripción o a la factura más reciente.
- **`src/app/api/stripe/webhook/route.ts`**
  - Al procesar `invoice.payment_succeeded`, se actualiza de manera persistente e inmediata la fecha del cobro real (`payment_completed_at`) y el cupón de descuento (`coupon_code`) en la tabla `users` de Supabase para todos los pagos.
  - Al cancelar o expirar una membresía, se envía el tag `miembro inactivo` mediante PUT, sobrescribiendo por completo los tags del contacto y removiendo de forma limpia la etiqueta `miembro activo` sin necesidad de endpoints de borrado secundarios.
- **`src/app/api/user/deactivate/route.ts`**
  - Se alineó el flujo de cancelación manual para que envíe `tags: ['miembro inactivo']` en lugar de invocar el método DELETE `removeContactTags` (el cual fue removido por obsoleto).
- **`src/app/api/admin/members/[id]/approve/route.ts`**
  - Al aprobar un miembro, se guardan en Supabase los campos de Stripe: primer pago en `payment_completed_at` y el cupón utilizado en `coupon_code`.

### 2. Mapeo y Visualización de Cupones en Reporte de Ventas
- **`src/app/api/admin/reports/plan-members/route.ts`**
  - Se añadieron `payment_completed_at` y `coupon_code` a las columnas devueltas por el API de reporte de miembros.
- **`src/components/Admin/Reports/PlanMembersReport.tsx`**
  - Se agregaron las columnas **F. Pago** y **Cupón** a la tabla de miembros en el panel administrativo.
  - Se añadieron **Fecha Pago**, **¿Utilizó Cupón?** (Sí/No) y **Cupón Utilizado** a la exportación en formato CSV solicitada por el equipo de ventas.
  - Se diseñó un distintivo estilizado premium (`🎟️ CUPON`) en la tabla para facilitar la identificación visual rápida de cupones activos.
- **`src/components/Admin/Reports/Reports.module.css`**
  - Se implementó la clase `.couponBadge` con estilo premium amigable y acorde al sistema de diseño del proyecto.

### 3. Ajuste de Tipos y Documentación
- **`src/services/crm.service.ts`**
  - Se cambió `addTags` a `tags` en `syncMembership` para forzar la sobrescritura y mantener consistencia con LeadConnector.
- **`Documentacion/Lynsales_Integracion_Resumen_Agencia.md`**
  - Se actualizó el resumen técnico con la resolución del borrado de tags (confirmado el comportamiento de overwrite) y se eliminaron las referencias a endpoints DELETE de etiquetas.

---

## 🚀 feat: Widget Home v2 para Webflow Basado en la Nueva Landing Page

### 1. Widget Principal y Previsualización
- **`public/widgets/home-widget-v2.js`**
  - Se creó el widget inyectable completo replicando con precisión la nueva landing page del repositorio `pata-amiga`.
  - Mapeo de variables CSS y diseño premium en base a Tailwind v4 (`.pata-v2-` prefijo para evitar colisiones).
  - Marquesina animada infinita para características de membresía.
  - Maquetación y comportamiento de los acordeones del FAQ.
  - Implementación asíncrona de los formularios de Wellness Leads (`/api/webflow/wellness-lead`) y Newsletter (`/api/webflow/newsletter`).
  - Redireccionamiento absoluto de enlaces de marketing a `https://www.pataamiga.mx/...` y links legales directos a PDFs de Webflow CDN.
  - Actualizados los enlaces de Embajadores para redirigir directamente a `https://www.pataamiga.mx/embajadores/dashboard`.
  - Configurado el enlace de Centros Aliados en el Nav del menú principal para realizar un scroll suave interno al formulario de Wellness Leads.
  - Actualizado el logo de marca `logoColorUrl` para usar la imagen local `logo-light-bg.svg` de la carpeta `home v2 images/` de forma dinámica y absoluta.
  - Integración del flujo de compras con Memberstack.
- **`public/widgets/home-widget-v2-preview.html`**
  - Página HTML de pruebas local que inyecta el widget de forma dinámica sobre el contenedor `#pata-home-widget`.
- **`tsconfig.json`** y **`.gitignore`**
  - Se excluyó la carpeta temporal `temp-pata-amiga/` de Git y de la compilación de TypeScript para evitar conflictos de imports en el build del proyecto principal.

---

## 📋 Notas técnicas y QA
- **Type checking**: `npm run type-check` ✅ (Completado con éxito, sin errores en ningún componente).
- **Compilación**: `npm run build` ✅ (Páginas estáticas y rutas compiladas exitosamente).
- **Linting**: `npm run lint` ✅ (0 errores, código limpio y consistente).
- **Knowledge Graph**: `graphify update .` ✅ (Grafo de conocimiento reconstruido correctamente).

---

*Generado por el agente — Club Pata Amiga*
