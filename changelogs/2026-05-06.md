# Changelog - 2026-05-06

## [1.2.3] - 2026-05-06

### ðŸŽ¨ UI/UX Improvements
- **Notification Icon Refresh**: Replaced the legacy bell emoji (`ðŸ””`) with a custom brand SVG across all notification widgets.
  - Updated `public/widgets/realtime-notifications.js` (Neo-Brutalist version).
  - Updated `public/widgets/notification-bell.js` (Standard version).
- **Transparency & Alignment**: Refined the bell's visual integration:
  - Set background to `transparent` and removed rigid button borders to rely on the SVG's internal circle.
  - Updated icon color to `#FFFFFF` for better visibility on dark/turquoise navbars.
  - Improved vertical alignment using `inline-flex` and `vertical-align: middle`.
  - Adjusted badge positioning for a cleaner, centered look.
- **SVG Optimization**: Implemented the SVG as a constant for maintainability, using `currentColor` to ensure proper color transitions.
- **Enhanced Toast & Modal**: Integrated the new custom icon into real-time toast notifications and the details modal for a consistent visual identity.

### âš™ï¸� Backend & Logic
- **Verified "Carencia" Logic**: Confirmed pet waiting period progress calculation in `unified-membership-widget.js` follows a 24-hour block system based on registration timestamps.
- **Dashboard Cleanup**: Temporarily hidden the invoice management box in the canceled plan view to streamline the user experience.

### ðŸš€ Registration Flow V2 Enhancements
- **Label Refinement**: Updated user-facing text in the registration funnel (`Step1Account.tsx`) to improve the "continuation" experience for returning users.
  - Header: "INICIA SESIÃ“N" -> "CONTINÃšA TU REGISTRO".
  - Button: "ENTRAR ðŸ�¾" -> "ENTRAR Y CONTINUAR ðŸ�¾".
  - Support: "Â¿Problemas para iniciar sesiÃ³n?" -> "Â¿Problemas para registrarte?".
- **Pantalla Intermedia de RedirecciÃ³n**: Se implementÃ³ una pantalla de transiciÃ³n ("Â¡Ya eres parte de la familia!") que se muestra durante 4 segundos antes de redirigir al portal oficial.
    - **Control de SesiÃ³n (Testing)**:
        - Se aÃ±adiÃ³ un botÃ³n de "Cerrar sesiÃ³n" en la pantalla de redirecciÃ³n para permitir a los testers cambiar de cuenta sin quedar atrapados en el bucle de redirecciÃ³n.
        - Se aÃ±adiÃ³ un link de logout en la cabecera del formulario (Paso 1) si detecta una sesiÃ³n activa.
    - **Refuerzo de DetecciÃ³n**: Ahora detecta planes activos mediante `planConnections`, `planIds` y `payment-status` custom field.
- **Salto de Pasos Inteligente (Corregido y Robustecido)**:
    - **Bug Fix**: La detecciÃ³n de mascotas buscaba en campos de la tabla `users` (`pet_name`, `pet_type`) que siempre estÃ¡n vacÃ­os. Las mascotas viven en la tabla separada `pets`.
    - **SoluciÃ³n**: Ahora se usa `getPetsByUserId()` que consulta directamente la tabla `pets` (fuente de verdad). Si el usuario tiene â‰¥ 1 mascota, se envÃ­a al **Paso 3** sin importar los metadatos de Memberstack.
    - **OptimizaciÃ³n**: En la carga inicial (`loadSavedState`), las consultas de usuario y mascotas se ejecutan en paralelo con `Promise.all()` para no aÃ±adir latencia.
- **Limpieza de UI**:
    - Se eliminÃ³ el botÃ³n de "Cerrar sesiÃ³n" redundante que flotaba sobre el formulario, ya que el Navbar ahora maneja esta opciÃ³n de forma consistente desde el Paso 1.
    - Se restaurÃ³ el ancho completo (`contentWide`) para el Paso 1.
- **Smart Redirection**: Implemented a security redirect in `NewRegistrationFlow.tsx` for already paid members.
  - If a member with an active plan (Monthly/Annual) attempts to log in through the registration flow, they are automatically redirected to the official login page (`https://www.pataamiga.mx/user/inicio-de-sesion`).
  - **Fix (Reinforcement)**: Made detection more robust by handling case-insensitive status (`active`/`ACTIVE`) and including trial status (`trialing`). Added console debug logs for real-time verification.
  - Added an exception for users who just completed a payment to ensure they can finish Step 4 (Profile) and Step 5 (Pets).
- **QA & Stability**: Verified full production build and type integrity for the updated registration logic.

---

## [1.2.4] - 2026-05-06 (commit: ce7fa40)

### ðŸ†• Widget: Perfil de Usuario

Se implementÃ³ el **User Profile Widget** completo (`public/widgets/user-profile-widget.js`) para embeber en Webflow.

#### SecciÃ³n 1 â€” InformaciÃ³n del Miembro
- Foto de perfil editable con upload directo a Supabase Storage (bucket `profile-photos`)
- Modal de ediciÃ³n en **columna Ãºnica** con campos: Nombre(s), Apellido paterno, Apellido materno, TelÃ©fono, Estado, Municipio/Ciudad, Colonia, CP, Fecha de nacimiento
- Scroll del fondo bloqueado mientras el modal estÃ¡ abierto

#### SecciÃ³n 2 â€” InformaciÃ³n de MembresÃ­a
- Stats: Plan activo, Costo, PrÃ³ximo pago
- **Logos SVG automÃ¡ticos** por marca de tarjeta: Mastercard, Visa, Amex, genÃ©rico como fallback
- Todos los botones de pago abren el Stripe Customer Portal via `$memberstackDom.launchStripeCustomerPortal()`
- CancelaciÃ³n con doble confirmaciÃ³n â†’ llama `/api/user/deactivate` â†’ logout

#### SecciÃ³n 3 â€” Roles Pata Amiga
- IntegraciÃ³n directa con `ManadaWidget` (pet-cards-widget.js) â€” toda la funcionalidad de tarjetas de mascotas incluida
- Vista de Embajador: cÃ³digo copiable, estadÃ­sticas de referidos (sin nombres por privacidad)

#### APIs Nuevas (build âœ…)
| Endpoint | MÃ©todo | DescripciÃ³n |
|----------|--------|-------------|
| `/api/user/profile` | GET | Datos del perfil desde Supabase |
| `/api/user/update-profile` | POST | Actualiza datos personales |
| `/api/user/payment-method` | GET | Tarjeta desde Stripe (server-side) |
| `/api/upload/profile-photo` | POST | Foto al bucket `profile-photos` |

#### Archivos entregados
- `public/widgets/user-profile-widget.js`
- `public/widgets/profile-widget-preview.html`
- 4 nuevas API routes en `src/app/api/`

- 4 nuevas API routes en `src/app/api/`

**Build**: 0 errores Â· **Retrocompatibilidad**: 100% (solo archivos nuevos)

---

## [1.2.4-hotfix] - 2026-05-06 (commit: 11c635e)

### ðŸ�› Bug Fixes: Profile Widget

**Fix 1 â€” Widget de mascotas no cargaba** (`pet-cards-widget.js`)
- **Causa**: El contenedor se renderizaba con `id="ppa-manada-container"` y luego se intentaba cambiar el id via JS, pero el `pet-cards-widget.js` ya habÃ­a iniciado su retry loop buscando `id="pata-amiga-manada-widget"` sin encontrarlo (39 intentos fallidos â†’ 500ms Ã— 39 â‰ˆ 20s de logs de error).
- **SoluciÃ³n**: `renderSection3()` ahora emite directamente `id="pata-amiga-manada-widget"` en el HTML. El retry loop del `pet-cards-widget` lo detecta en el siguiente ciclo (â‰¤ 500ms) y se inicializa solo â€” sin intervenciÃ³n manual.
- **Eliminado**: `initManadaWidget()` y toda la lÃ³gica de instanciaciÃ³n manual de `ManadaWidget` que causaba doble inicializaciÃ³n.

**Fix 2 â€” Error 404/500 en embajadores**
- **Causa**: El widget llamaba a `/api/ambassadors/profile` que no existÃ­a.
- **SoluciÃ³n**: Creado endpoint nuevo `GET /api/ambassadors/by-memberstack?memberstackId=` que consulta Supabase por `memberstack_id` sin requerir token de sesiÃ³n. Devuelve `{ ambassador: null }` si el usuario no es embajador (sin error), o el objeto embajador con estadÃ­sticas de referidos si sÃ­ lo es.

**Archivos modificados**
- `public/widgets/user-profile-widget.js` â€” fix container id + eliminar initManadaWidget
- `src/app/api/ambassadors/by-memberstack/route.ts` â€” nuevo endpoint (GET)

---

## [1.2.5-hotfix] - 2026-05-06 (commit: a007f4a)

### ðŸ�› Bug Fixes: Profile Data & Membership Sync

**Fix 3 â€” Datos personales vacÃ­os / Nombre incorrecto**
- **Causa**: El API `/api/user/profile` fallaba silenciosamente (error 400 de PostgREST) porque el `SELECT` incluÃ­a `paternal_last_name` y `maternal_last_name`, columnas que no existen en la base de datos (los nombres correctos son `last_name` y `mother_last_name`). Al fallar el API, el widget usaba el email como nombre fallback y mostraba los campos personales vacÃ­os.
- **SoluciÃ³n**: Se actualizaron las referencias de campos en el API GET, API POST y en el widget JS (modal y renderizado) para usar `last_name` y `mother_last_name`.
- **Mejora**: Se simplificÃ³ la visualizaciÃ³n de la direcciÃ³n en el widget para mostrar solo `colonia, ciudad, estado, C.P.`, alineÃ¡ndose con los datos capturados en el registro.

**Fix 4 â€” Fecha de prÃ³ximo pago no disponible**
- **Causa**: El campo `next_payment_date` en Supabase suele estar vacÃ­o o desactualizado hasta que Stripe notifica cambios.
- **SoluciÃ³n**: El endpoint `/api/user/payment-method` ahora consulta en tiempo real la suscripciÃ³n activa del cliente en Stripe, extrae el `current_period_end` y lo devuelve al widget. AdemÃ¡s, lo sincroniza en Supabase para futuras consultas.

**Archivos modificados**
- `public/widgets/user-profile-widget.js` â€” ActualizaciÃ³n de nombres de campos y lÃ³gica de direcciÃ³n.
- `src/app/api/user/profile/route.ts` â€” Fix en query de Supabase.
- `src/app/api/user/update-profile/route.ts` â€” Fix en nombres de campos de actualizaciÃ³n.
- `src/app/api/user/payment-method/route.ts` â€” IntegraciÃ³n de fecha de suscripciÃ³n Stripe.

---

## [1.2.6-hotfix] - 2026-05-06 (commit: f91120f)

### ?? Bug Fixes: Schema Alignment & Stripe Sync

**Fix 5 — Errores SQL 42703 (Column Not Found)**
- **Causa**: Varias rutas de API realizaban consultas a columnas inexistentes en la tabla 'users' (profile_photo_url, plan_name, next_payment_date, street, full_name), lo que provocaba que el widget de perfil mostrara información vacía o fallara al cargar.
- **Solución**:
    - Se eliminaron todas las referencias a columnas de metadatos de pago en la tabla 'users'. Ahora, el costo del plan, el tipo (Anual/Mensual) y la fecha de próximo pago se obtienen **en tiempo real** desde Stripe a través del endpoint '/api/user/payment-method'.
    - Se migró el campo de foto de perfil de 'profile_photo_url' a 'avatar_url' para coincidir con el esquema real de Supabase.
    - Se corrigió la consulta de logs de apelación ('/api/admin/members/[id]/appeal-logs') eliminando la columna 'full_name' y calculando el nombre dinámicamente a partir de 'first_name' y 'last_name'.
    - Se eliminó el campo 'cancelled_at' en '/api/user/deactivate' para evitar errores durante la cancelación.

### ?? Profile Widget UI Refinement
- **Layout del Modal**: Se rediseñó el modal de edición de datos para usar una estructura **vertical (una sola columna)**, evitando el solapamiento de campos en resoluciones intermedias.
- **Bloqueo de Scroll**: Se implementó el bloqueo de scroll del fondo ('overflow: hidden') al abrir el modal para mejorar la experiencia de usuario (UX).
- **Formateo de Membresía**: Se corrigió el formato de los costos (ej. **,699** o ****) y se muestran los tipos de plan como 'Anual' o 'Mensual' según el intervalo de Stripe.
- **Mapeo de Dirección**: Se aseguró que los campos de dirección en el widget mapeen correctamente a 'colony', 'city', 'state' y 'postal_code'.

**Archivos modificados**
- 'src/app/api/user/profile/route.ts' — Eliminación de columnas obsoletas.
- 'src/app/api/user/update-profile/route.ts' — Mapeo a 'avatar_url'.
- 'src/app/api/user/payment-method/route.ts' — Enriquecimiento de datos desde Stripe (costo, intervalo, fecha).
- 'src/app/api/user/deactivate/route.ts' — Remoción de 'cancelled_at'.
- 'src/app/api/admin/members/[id]/appeal-logs/route.ts' — Remoción de 'full_name'.
- 'public/widgets/user-profile-widget.js' — Sync con nuevo esquema, layout vertical y scroll lock.

---

## [1.2.7-hotfix] - 2026-05-06 (commit: 36cb06e)

### 🐾 Bug Fixes: Membership Data Enrichment

**Fix 6 - Membership Cost & Payment Date Sync**
- **Causa**: El API `/api/user/payment-method` no estaba extrayendo los detalles de la suscripción activa (costo, intervalo y nombre del plan), devolviendo solo la fecha de próximo pago en un formato (`camelCase`) que el widget no reconocía (`nextPaymentDate`).
- **Solución**: 
    - Se actualizó el endpoint para realizar una consulta completa a la suscripción en Stripe.
    - Ahora extrae y devuelve `plan_cost`, `interval` ("Anual"/"Mensual") y `plan_name` directamente desde el objeto `Price` de Stripe.
    - Se estandarizó la respuesta a `snake_case` (`next_payment_date`) para sincronización perfecta con el widget.
- **Impacto**: Los usuarios ahora ven su costo real (ej. $1,699), el tipo de membresía correcto y la fecha de su siguiente cargo en lugar de guiones o placeholders.

**Archivos modificados**
- `src/app/api/user/payment-method/route.ts` — Enriquecimiento completo de datos de membresía desde Stripe.

---

## [1.2.8-hotfix] - 2026-05-06 (commit: e9b0b48)

### 🐾 Bug Fixes: Next Payment Date & Widget Diagnostics

**Fix 7 - Next Payment Date Rendering**
- **Causa**: La fecha de próximo pago no se mostraba a pesar de estar presente en el API, debido a una falta de robustez en el formateo de fechas del widget y a una búsqueda demasiado restrictiva en la API de Stripe.
- **Solución**: 
    - **API**: Se amplió la búsqueda en `/api/user/payment-method` para incluir múltiples registros y se añadieron logs de servidor para monitorear el valor de `current_period_end`.
    - **Widget**: Se robusteció la función `fmtDate` en `user-profile-widget.js` para manejar valores nulos, indefinidos o fechas inválidas de forma segura.
    - **Diagnóstico**: Se inyectó un logger de depuración (`[DEBUG] PaymentMethod Info`) que imprime los datos de membresía directamente en la consola del navegador del usuario.
- **Impacto**: Corrección del error de visualización de la fecha de pago y visibilidad total del flujo de datos desde Stripe hasta el navegador.

**Archivos modificados**
- `public/widgets/user-profile-widget.js` — Fix `fmtDate` y logger de depuración.

---

## [1.2.9-hotfix] - 2026-05-06 (commit: 56b9ff3)

### 🐾 Bug Fixes: API Scope & Subscription Broadening

**Fix 8 - API ReferenceError & Status Broadening**
- **Causa**: Un error de alcance (`ReferenceError`) en `/api/user/payment-method` impedía que la información de depuración y la fecha se enviaran correctamente al widget. Además, la búsqueda de suscripciones era demasiado restrictiva al filtrar solo por `active`.
- **Solución**: 
    - **Fix Crítico**: Se corrigió el orden de declaración de variables para evitar el `ReferenceError` al asignar el objeto de depuración.
    - **Broadening**: Ahora se consultan **todas** las suscripciones del cliente (`status: 'all'`) y se filtran dinámicamente (`active` o `trialing`) para asegurar que no se pierda ninguna membresía válida.
    - **Debug**: Se expandió el objeto `_debug_sub` para incluir el ID de suscripción y el estado real reportado por Stripe.
- **Impacto**: Estabilización del API de métodos de pago y visibilidad total sobre suscripciones en cualquier estado válido.

**Archivos modificados**
- `src/app/api/user/payment-method/route.ts` — Fix de scope y lógica de filtrado de suscripciones.
- `public/widgets/user-profile-widget.js` — Actualización del logger para mostrar datos de depuración extendidos.
