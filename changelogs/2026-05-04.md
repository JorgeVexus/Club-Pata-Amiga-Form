# Changelog - 2026-05-04

## [1.2.0] - 2026-05-04

### Seguridad Administrativa (Zero Trust)
- **Migración Completa a `adminFetch`**: Se ha completado la migración de todos los componentes del dashboard administrativo a la utilidad segura `adminFetch`.
    - Blindaje de `AdminsTable.tsx` para la gestión de usuarios administrativos.
    - Asegurado `BillingManagement.tsx` para datos financieros y de Stripe.
    - Protegido `MessageSender.tsx` para comunicaciones y eliminación de miembros.
    - Estandarización de `AdminDashboard.tsx` eliminando wrappers de fetch obsoletos.
- **Validación de Headers**: Implementación obligatoria del header `x-admin-memberstack-id` en todas las peticiones a `/api/admin/*`.
- **QA & Integridad**: Verificación de tipos con `tsc` y limpieza de referencias circulares en el build de producción.

### Correcciones
- **MessageSender**: Se corrigió error de compilación por falta de imports de acciones de servidor (`sendAdminEmail`, `sendCustomNotification`).

---
*Blindaje de seguridad nivel producción completado.*
