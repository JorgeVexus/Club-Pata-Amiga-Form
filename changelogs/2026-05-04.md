# Changelog - 2026-05-04

## 🚀 Finalización del Flujo de Registro V2 (Estandarización y QA)

Se ha completado la unificación total del embudo de registro `RegistrationV2`, consolidando la identidad visual **neo-brutalista** en todo el embudo, resolviendo errores de runtime y optimizando la persistencia de datos de facturación.

### 📋 Cambios Principales

#### 🎨 Rediseño Visual Estandarizado (Neo-Brutalismo)
- **Step 6 Success**: Rediseño completo de la pantalla de éxito.
  - Implementación de `formCard` con bordes de 2px, sombras de 4px y radios de 48px.
- **Billing Modal**: Mejora estética y funcional del modal.
  - **Ajuste de UI**: Los campos de C.P. y Email de envío ahora se muestran en vertical para mejorar la experiencia de usuario.
  - Tipografía `Fraiche` para títulos y `Outfit` para cuerpos de texto.

#### 🛠️ Optimizaciones Técnicas y Fixes
- **TypeError Fix**: Se resolvió el error `Cannot read properties of undefined (reading 'call')` en el Step 6 mediante:
  - Refactorización de importaciones de `MetaPixel` a named imports directos.
  - Validación robusta de `window.$memberstackDom` antes de llamadas a métodos de logout.
- **Lógica de Facturación**:
  - Mapeo robusto en `saveBillingDetailsByMemberstackId` para soportar campos en español (`cp`, `razonSocial`, `regimenFiscal`).
  - Persistencia vinculada al `memberstack_id` mediante server actions de Supabase.

#### ✅ Control de Calidad (QA)
- **Build Verification**: El proyecto compila correctamente (`npm run build`).
- **Type Checking**: Pasado exitosamente sin errores de tipos en los nuevos componentes.

---
**Impacto funcional**: El flujo de registro ahora es visualmente consistente, técnicamente más estable frente a recargas de página y asegura que los datos fiscales se guarden correctamente independientemente del idioma de los campos en el frontend.
