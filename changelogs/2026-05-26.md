# Changelog — 2026-05-26

## ✅ Visualización de carencia de mascotas en dashboard admin

- **src/components/Admin/MemberDetailModal.tsx**:
  - Se importaron las utilidades de carencia centralizadas `getPetCarenciaDate`, `getDaysUntilActive` y `getDaysElapsed`.
  - Se extendió la interfaz `Pet` para incluir opcionalmente `waiting_period_start` e `is_mixed`.
  - Se añadieron cálculos dinámicos de carencia en el mapeo de mascotas (`hasAmbassadorCode`, `totalCarenciaDays`, `daysElapsed` y `daysRemaining`).
  - Se renderizó en la sección `detailsGrid` de cada mascota el tiempo de carencia total (indicando si es estimado), la fecha de inicio de carencia (o indicando si está pendiente), y los días transcurridos y restantes cuando está aprobada.

- **Documentacion/planeacion/2026-05-26-mostrar-dias-carencia-admin.md**:
  - Se creó el plan de implementación para la visualización del período de carencia de las mascotas.

## ✅ Corrección de bug crítico en cálculo de carencia al aprobar mascotas

- **src/app/api/admin/members/[id]/pets/[petId]/status/route.ts**:
  - Se removió la columna inexistente `waiting_period_days` de la consulta `.select()` de la mascota previa, la cual causaba que la consulta fallara silenciosamente y devolviera `null` para la mascota, aplicando por defecto los 180 días de carencia a todas las mascotas independientemente de su estado de adopción y raza.
  - Se agregó una validación de errores a la consulta para lanzar un error explícito si la obtención falla.
  - Se actualizó el cálculo de días de carencia removiendo la referencia a `waiting_period_days`.

## QA & Build
- `npm run type-check` ✅
- `npm run lint` ✅
- `npm run build` ✅

## ✅ Ajuste de reglas de carencia para mestizos/gatos y recálculo de base de datos

- **src/utils/carencia.utils.ts**:
  - Se actualizó la interfaz `CarenciaInput` para incluir `breed` y `pet_type`.
  - Se modificó la función central `getPetCarenciaDate` para considerar adoptada por defecto (`isAdopted = true`) a cualquier mascota mestiza (raza con "mestizo" o "doméstico") o gato (`pet_type` es "cat" o "gato"). Esto les otorga un periodo de 120 días (mestizo) o 150 días (raza pura si aplica) en lugar de los 180 días por defecto.
- **src/app/api/admin/members/[id]/pets/[petId]/status/route.ts**:
  - Se seleccionan las columnas `breed` y `pet_type` de la mascota en Supabase.
  - Se migró el cálculo de la fecha de finalización al aprobar una mascota para usar la función unificada `getPetCarenciaDate`, garantizando consistencia en futuras aprobaciones manuales desde el panel de administración.
- **Base de datos (Supabase)**:
  - Se ejecutó con éxito un script de recálculo masivo para mascotas con fecha de creación entre el 15 y el 27 de mayo de 2026.
  - Se corrigió y actualizó el `waiting_period_end` de **6 mascotas** aprobadas (incluyendo gatos y mestizos que tenían configurados 180 días de carencia por no marcarse explícitamente como adoptadas) reduciéndolas a 120 días.

## ✅ Reemplazo de términos de carencia por tiempo de espera en el dashboard admin

- **src/components/Admin/MemberDetailModal.tsx**:
  - Se modificaron las etiquetas en el modal de detalle del miembro para cambiar "carencia" por "tiempo de espera", mejorando la legibilidad.
  - "⏱️ Tiempo de carencia" → "⏱️ Tiempo de espera".
  - "📅 Inicio de carencia" → "📅 Inicio de tiempo de espera".

## ✅ Actualización en tiempo real del Dashboard Admin sin recargar página

- **src/components/Admin/RequestsTable.tsx**:
  - Se añadió la propiedad reactiva `refreshKey` para permitir el refresco de solicitudes y estadísticas de manera local e in-place.
- **src/components/Admin/WellnessCentersTable.tsx**:
  - Se añadió la propiedad reactiva `refreshKey` para refrescar estadísticas y listado de aliados de bienestar.
- **src/components/Admin/AmbassadorsTable.tsx**:
  - Se añadió la propiedad reactiva `refreshKey` para refrescar estadísticas y listado de embajadores.
- **src/components/Admin/AdminDashboard.tsx**:
  - Se implementó el estado reactivo `refreshKey` y la función helper `triggerInPlaceRefresh` que actualiza métricas, feed de actividades y contadores sin recargas de página.
  - Se eliminaron las llamadas a `window.location.reload()`.
  - Se ajustó el callback `onApprove` y `onDataChange` para miembros, de tal forma que se invoque a `fetchMemberDetails` para actualizar la información del modal in-place.
  - Se adaptaron los modales de embajadores y aliados para refrescarse in-place.
- **src/components/Admin/MemberDetailModal.tsx**:
  - Se añadió la llamada a `onDataChange()` tras enviar una respuesta de apelación para mantener la tabla de fondo sincronizada.
- **Documentacion/planeacion/2026-05-26-actualizacion-modal-sin-recarga.md**:
  - Se documentó el plan de diseño y pruebas para refrescos en tiempo real.

## ✅ Botón de recarga forzada en Navbar Admin

- **src/components/Admin/Navbar.tsx**:
  - Se implementó un botón de recarga (índice 🔄) con feedback visual de carga.
  - El botón despacha un evento global `force-refresh-dashboard`.
- **src/components/Admin/Navbar.module.css**:
  - Se añadieron estilos personalizados para el botón de recarga (circular, hover interactivo, bordes definidos).
- **src/components/Admin/AdminDashboard.tsx**:
  - Se añadió un listener de eventos para `force-refresh-dashboard` que invoca `triggerInPlaceRefresh()`, permitiendo a los administradores invalidar el caché visual sin recargar toda la pestaña.
- **force-refresh.html**:
  - Se añadió un script de utilidad HTML para depuración y recargas externas rápidas.
- **scripts/force-refresh-members.js**:
  - Se creó un script de node para recargar datos de miembros vía API de Memberstack si se requiere sincronización manual pesada.

## ✅ Corrección de inconsistencia de nombres en lista de miembros

- **src/app/api/admin/members/route.ts**:
  - Se actualizó el proceso de enriquecimiento para obtener `first_name` y `last_name` directamente de Supabase para cada miembro listado.
- **src/components/Admin/RequestsTable.tsx**:
  - Se modificó la lógica de renderizado del nombre para priorizar los campos de Memberstack, pero utilizar los nombres de Supabase como respaldo si los primeros están vacíos. Esto elimina el problema de "Sin nombre" en la tabla principal cuando el modal de detalles sí muestra la información.

## ✅ Nuevo Widget: Completar Perfil para Webflow (v1.1)

- **public/widgets/complete-profile-widget.js**:
  - Implementación de pre-llenado de campos: el widget ahora carga y muestra la información existente en Supabase.
  - Lógica de detección de pasos optimizada: solo se muestran formularios si faltan datos obligatorios.
  - Formulario de registro de mascota extendido: se añadieron campos de historia de adopción, tamaño de raza y validación de certificado para mascotas Senior.
  - Agregados logs de depuración para facilitar el seguimiento en producción.
- **src/app/api/upload/document/route.ts**:
  - Nueva API para la gestión de subidas de documentos generales.
- **src/app/api/user/profile/route.ts** & **src/app/api/user/update-profile/route.ts**:
  - Se ampliaron los endpoints para soportar CURP, género, nacionalidad y URLs de documentos legales.
- **public/widgets/complete-profile-preview.html**:
  - Página de previsualización e instrucciones técnicas de integración.


